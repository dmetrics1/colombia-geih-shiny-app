.Rproj.user
.Rhistory
.RData
.RDataTmp
.Ruserdata

# Credenciales — NUNCA versionar (token/secret de shinyapps.io)
clave.R
.Renviron

# Datos pesados — se descargan del DANE y se regeneran con join_geih.R (no se versionan)
datos/
geih_complete.csv

# Copias de trabajo / scratch
tes/

# Artefactos locales de desarrollo / preview (rutas específicas de la máquina)
.claude/launch.json
tests/_*.R

# Metadata de despliegue de rsconnect (específica de la máquina/cuenta)
rsconnect/
